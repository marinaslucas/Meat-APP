export interface Restaurant { //colocar os dados pros restaurantes. Interfaces: pode ter apenas assinaturas, nao pode ter implementação de métodos.A lém disso, nao posso instanciar objetos, pq nao eh uma
   id: string
   name: string
   category: string
   deliveryEstimate: string
   rating: number
   imagePath: string
 }
